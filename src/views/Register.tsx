import { Button } from 'antd';
import * as React from 'react';

class Register extends React.Component<object, object> {
  public render() {
    return (
      <div className="Register">
        <Button type="primary">Register</Button>
      </div>
    );
  }
}

export default Register;
